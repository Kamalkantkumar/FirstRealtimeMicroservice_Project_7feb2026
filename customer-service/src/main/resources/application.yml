# ===================================================================
# CUSTOMER SERVICE - LOCAL BOOTSTRAP INITIALIZER
# ===================================================================
spring:
  application:
    name: customer-service

  profiles:
    active: dev

  config:
    # Tells Spring to pause boot and download settings from port 8888
    import: "optional:configserver:http://localhost:8888"